#include "main.h"
/**
 * create_array - variable
 * @size: tamaño
 * @c: caracter
 * Return: always 0.
 **/
char *create_array(unsigned int size, char c)
{
	char *ptr;
	unsigned int i, n;

	*ptr = malloc(n * sizeof(char);

	if (ptr == NULL)
	{
		exit(0);
	}
	else if (size != 0)
	{
		for (i = 0; i < n; i++)
			ptr[i] = c
		return (ptr);
	}
	else
	{
		return (NULL);
	}
}
