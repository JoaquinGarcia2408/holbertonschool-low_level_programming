#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 *  *  * main - Entry point
 *   *   * Return: Always 0 (Success)
 *    *    **/
int main(void)
{
	int num = 0;

	while (num > 8)
	{
		printf("%d\n", num);
		num++;
	}
	return (0);
}
